export type SortVizProps = {
    min: number;
    max: number;
    width: number;
    height: number;
};

export type SortVizState = {
    currArray: number[];
    barColours: number[];
    size: number;
};
