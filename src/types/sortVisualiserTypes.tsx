export type sortVizProps = {
    size: number;
    min: number;
    max: number;
};

export type sortVizState = {
    array: number[];
    width: number;
};

export type animation = {
    firstIdx: number;
    secondIdx: number;
};
